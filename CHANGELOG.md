# Changelog

本项目的显著变更记录在此。版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [1.6.0] - 2026-09-19

### 阅读界面

- 阅读时隐藏系统状态栏及导航栏，边缘滑动可临时唤出，离开阅读后恢复系统栏；继续按系统安全区避让刘海和打孔。
- 底部左侧显示电量图标、百分比及系统格式的时间，右侧显示本章当前页 / 总页数；滚动模式显示当前段 / 总段数。
- 底栏至少左右内缩 28dp、底部上移 20dp；Android 12 及以上结合系统报告的底部圆角半径增加内缩，并避让底部安全区。
- 底栏独立参与阅读布局测量，正文按剩余高度重新分页，避免遮挡末行；时间和电量随系统广播更新，离开前台停止监听。

### 兼容与验证

- 时间遵循手机的 12/24 小时格式，响应分钟、时间及时区变化；重新进入前台立即更新。充电时电量百分比附加加号标记。
- 保留顶部书名、章节与简繁转换；底部页码统计范围仍为当前章节，不预加载整本书计算总页数。
- 版本升级为 `versionName 1.6.0`、`versionCode 10`，沿用现有签名，支持保留数据覆盖升级。
- 单元测试、静态检查及签名 APK 构建通过；Xiaomi 25042PN24C 真机已验证沉浸显示、圆角避让、电量/时间、页码随翻页更新及退出阅读后系统栏恢复，用户已确认显示效果。
- 横屏、非充电状态及系统大字体尚未进行专项实机测试。

## [1.5.0] - 2026-09-03

### 新增

- “我的”页面底部居中显示当前 App 版本号，自动读取构建版本，游客和登录状态下均可查看。
- 阅读界面顶部常驻当前书名和章节名，使用紧凑信息栏；长标题省略展示，点击可查看完整内容。
- 信息栏位于系统状态栏与刘海安全区域下方，横屏也避让侧边挖孔；左右翻页和上下滚动模式均可见，操作菜单仍默认隐藏。
- 阅读设置新增“原文 / 简体 / 繁体”选项，使用成熟开源库 opencc4j 1.14.0 离线进行字词级转换，选择保存在本机。
- 简繁转换覆盖书名、章节、正文和链接文字；图片地址、下载目标和缓存原文保持不变，随时可以切回原文。

### 优化

- 简繁转换在后台线程进行，转换时保留已有内容；转换完成后重新分页，按原段落定位。
- 阅读设置内容可滚动，小屏幕或大字号下仍可访问所有设置。
- 重新分页导致页数减少时不再自动触发下一章；只有明确的翻页手势或下一章按钮才切章。
- 单独更改本地简繁模式不会调用网站阅读设置保存接口。

### 兼容、安全与验证

- 默认版本升级至 `versionName 1.5.0`、`versionCode 9`；“我的”页面显示的是实际构建版本，后续发布时自动更新。
- Release 流程使用项目配置中的内部版本代码，不再以工作流运行次数覆盖；本地与 CI 的 1.5.0 均使用 `versionCode 9`，避免测试包与正式包升级顺序冲突。
- 沿用已有 Release 签名，可覆盖安装并保留登录状态、书架缓存和阅读设置。
- 简繁转换完全在设备本地完成，不上传正文；每次均从源内容转换，避免反复切换造成文字损失，纯网址标签保持原样。
- APK 随包保留 opencc4j 及依赖的开源声明、Apache 2.0 许可证；本项目继续使用 MIT 协议。
- 新增 9 项单元测试，覆盖简繁与词组转换、混合文字、空内容、链接/插图不变、原文恢复、旧设置兼容，以及重新分页的段落定位和页码边界；全量 51 项测试通过。
- 已在 Xiaomi 25042PN24C 真机验证：顶部安全区与长标题查看、分页/滚动、简转繁及繁转简、重启后的设置保留、切换后停留当前页、页码输入、末页继续进入下一章和上一章按钮。
- “我的”页面底部版本号已实机确认居中显示；测试使用同签名覆盖安装，没有卸载 App 或清除登录状态。
- 横屏和系统大字体尚未进行专项实机验收；已采用系统安全区和可滚动设置布局。

## [1.4.1] - 2026-09-03

### 新增

- 阅读器保留正文中的 HTML 链接和纯文本网址，使用明显的可点击样式展示。
- 正文链接会区分站内页面、直接文件与站外网盘；直接文件可加入 Android 系统下载任务，站外地址会先展示真实域名再交给浏览器或网盘 App。
- 轻币章节展示真实价格、当前账号轻币余额和刷新入口；支付通过轻之国度官方网页完成，返回 App 后自动刷新章节权限和余额。
- 图书详情页新增分享入口，可复制 `https://www.lightnovel.fun/book/{bookId}` 原始网页地址或调用 Android 系统分享面板。

### 修复

- 修复目录把所有 `locked` 章节直接禁用、点击后没有反馈的问题；目录章节现在始终允许尝试打开，再由章节详情中的访问类型决定正常阅读、轻币解锁或错误提示。
- 不再把 `unlocked=false` 无条件折叠为普通“锁定”，完整保留 `accessType`、`unlocked` 和 `coinPrice`，可区分公开章节与轻币章节。
- 分页阅读的点击手势会尊重正文链接的点击消费，点击下载/网盘链接时不会同时触发翻页。
- 修复非轻币锁定章节被误判为公开章节、进入后只显示“本章暂无正文”的问题；目录现在标记为“访问受限”，空详情会明确提示“没有权限访问该章节，或内容已删除”。

### 安全与验证

- 拒绝 `javascript:`、`data:`、`file:`、`content:`、`intent:` 等危险链接；相对地址只按轻之国度 HTTPS 基址解析，仿冒域名不会被认作站内地址。
- 轻币扣款不使用未验证的猜测接口；官方网页在用户最终确认后执行支付，App 不自动点击、不自动重试扣款。
- 缓存仍显示“需轻币”的章节在每次进入时都会后台复核服务器权限，支持从系统浏览器、进程重启或其他设备完成购买后恢复正文。
- 目录接口没有返回个人解锁状态时使用中性的“轻币章节”标记，避免已购买章节仍被误写为“需轻币”。
- 单元测试覆盖轻币访问状态、链接解析、相对下载地址、站外网盘、仿冒域名和危险 scheme。
- 小米 Android 16 真机验证通过：用户在官方网页手动解锁 20 轻币章节后，余额由 370 更新为 350；覆盖安装并重新进入时旧锁定缓存自动刷新为 3 页正文，没有再次扣款。
- 真机复核书籍 `327` 的章节 `259918`：目录元数据属于无价格的受限章节，详情为空时明确显示“没有权限访问该章节，或内容已删除”，不再伪装为正常空正文。
- 另以《才女的侍从[EPUB]》第二卷验证真正的未购买章节：App 显示价格 20 轻币和余额 350 轻币；验收在 App 内取消，未打开支付网页、未产生扣款。
- 真机识别章节末页 `cf-pan.acgdmzy.com` 站外下载地址，点击后先显示真实域名和确认框；测试选择取消，未启动下载。图书分享生成的原始地址确认为 `https://www.lightnovel.fun/book/436`。
- 默认版本升级至 `versionName 1.4.1`、`versionCode 8`。

## [1.3.0] - 2026-08-09

### 新增

- 新增内存与 SQLite 两级内容缓存，采用 stale-while-revalidate：页面优先展示缓存，过期后在后台刷新，网络结果就绪后原位替换。
- 新增 96 MiB 统一磁盘缓存上限和按最后访问时间淘汰的 LRU 策略，避免正文与列表缓存无限增长。
- 新增章节正文离线缓存，在线打开过的章节可在无网络时继续阅读；正文默认缓存 7 天并参与 LRU 淘汰。
- 发现、搜索分类与结果、书籍详情、分卷、章节目录、评论、书架、个人资料、关注/粉丝、阅读记录和发布管理均支持磁盘恢复。
- 消息未读、通知列表和私信加入 30 秒进程内缓存，不把消息正文或私信写入磁盘。
- 无缓存的首次加载使用稳定骨架占位；有缓存时后台刷新只显示顶部细进度条，不再用全页转圈覆盖内容。
- 主要页面新增显式“刷新”入口；手动刷新始终请求服务器，同时保留当前内容。

### 缓存与隐私

- 公共内容与登录数据使用独立作用域；书架、个人资料、关注/粉丝、阅读记录和发布管理按 `user:{uid}` 隔离。
- 缓存键不包含密码、验证码或 `security_key`；退出登录会清除所有用户私有缓存，公开内容和正文缓存保留。
- 内容缓存数据库明确排除在 Android 云备份和设备迁移之外，缓存正文只保留在当前设备。
- 缓存 JSON 无法解析时只删除损坏条目并重新请求，不影响应用启动或其他缓存。
- 加入/移出书架、关注切换、删除阅读记录、保存阅读进度和消息标为已读后，会失效对应缓存以避免展示旧状态。

### 改进

- 页面状态统一区分首次加载、后台刷新、阻塞错误和非阻塞刷新错误；刷新失败时保留可用缓存并明确提示。
- 同一资源的并发请求使用互斥去重，页面快速往返不会重复刷新同一份数据。
- 正文后台刷新不遮挡阅读页面，缓存正文、上一章/下一章导航信息和插图标记一并恢复。
- 默认版本升级至 `versionName 1.3.0`、`versionCode 6`。

### 验证

- 单元测试覆盖新鲜缓存、过期缓存、强制刷新、断网回退、损坏条目恢复、用户作用域清理与 LRU 淘汰顺序。
- `testDebugUnitTest`、`lintDebug`、`assembleDebug`、签名 `assembleRelease` 全部通过；Release APK 的 v2 签名验证通过。
- 小米 25042PN24C、Android 16 使用 `adb install -r` 覆盖安装成功，1.2.0 登录态和应用数据完整保留。
- 真机杀进程并同时关闭 Wi-Fi/移动数据后，发现页可直接从磁盘恢复；已在线读过的 27 页章节可从缓存书架和详情离线进入并完整显示。
- 书架缓存过期刷新期间仍持续显示原列表，只在顶部展示细进度条；断网失败不会用全屏加载覆盖已有内容。
- 详细设计、TTL、作用域、失效规则和真机验收标准见 `docs/CACHE_AND_REFRESH_PLAN.md`。

## [1.2.0] - 2026-08-08

### 新增

- “我的”页面新增消息中心入口和总未读徽标；未读汇总失败不会影响个人资料与其他账户功能加载。
- 新增私信、回复、@我、点赞、新粉丝、系统六个消息分类，分类标签显示各自未读数量并支持横向滚动。
- 通知卡展示来源用户/系统、标题、正文、引用内容、关联作品和时间；未读通知使用独立背景色。
- 支持通知分页加载，首屏错误、加载更多错误与“标为已读”操作错误分别处理。
- 支持从含作品目标的通知跳转到书籍详情或指定章节；无法识别的动态、评论目标保持只读，不构造错误路由。
- 新增私信会话列表与只读消息线程，显示对方、消息方向、内容和时间；明确说明本版本不会发送私信。
- 新增显式“标为已读”操作；进入页面或切换分类不会自动改变站点消息状态。

### API

- `api/bff/message-unread-v1`：总未读和六类未读汇总。
- `api/bff/message-replies-v1`：回复与 @；@ 分类携带 `filter: mention`。
- `api/bff/message-likes-v1`、`api/bff/message-fans-v1`、`api/bff/message-system-v1`：点赞、新粉丝和系统通知。
- `api/bff/dm-conversations-v1`、`api/bff/dm-messages-v1`：私信会话与只读线程。
- `api/bff/message-mark-read-v1`、`api/bff/dm-mark-read-v1`：用户显式触发的分类已读操作。

### 改进

- 通知兼容多种消息 ID、正文、来源用户、引用、目标与时间字段；保留书籍、卷、章节、动态、评论和回复目标 ID。
- 私信兼容会话、用户、最后消息、时间和消息方向的历史字段；当站点会话接口不提供摘要时显示“点击查看会话”。
- 消息中心的空状态按分类显示，不把“没有消息”误报为网络失败。
- 默认版本升级至 `versionName 1.2.0`、`versionCode 5`。

### 验证

- `testDebugUnitTest`、`lintDebug`、`assembleDebug` 通过，测试覆盖六类未读数、通知目标、嵌套私信摘要、会话和消息方向解析。
- 小米 25042PN24C、Android 16 真机覆盖安装验证通过，登录态完整保留。
- 回复、@我、点赞、新粉丝接口正确显示各自空状态；系统分类成功加载并展示 3 条历史通知。
- 私信分类成功加载现有会话并进入只读线程，消息内容、发送方和时间显示正常。
- 验证过程只执行只读浏览和分类切换，未点击“标为已读”、未发送私信，也未改变账户数据。

### 已知限制

- 私信发送、通知内直接回复、动态与评论精确定位尚未实现。
- 当前测试账户没有未读消息及带书籍/章节目标的通知，因此已读写接口与真机目标跳转仅由代码审查和单元测试覆盖，未执行账户写入式验收。

## [1.1.0] - 2026-08-08

### 新增

- “我的”页面升级为登录用户仪表盘，展示头像、昵称、UID、签名、用户组、轻币、关注数、粉丝数和发布数。
- 新增关注与粉丝页面：
  - 支持关注/粉丝双 Tab 与真实数量。
  - 展示头像、昵称、等级、签名和当前关注关系。
  - 支持显式关注和取消关注；取消关注前二次确认，防止误操作。
  - 支持分页加载，并将首屏错误、加载更多错误和单项操作错误分离。
- 新增阅读记录页面：
  - 展示封面、作品、最后阅读章节和阅读时间。
  - 有章节定位时直接继续阅读，否则进入书籍详情。
  - 支持删除单条云端阅读记录，删除前二次确认。
- 新增发布管理页面：
  - 展示用户发布作品、类型、连载状态、审核进度、卷章数、字数和更新时间。
  - 点击作品进入公开书籍详情。
  - 完整作者编辑工作台仍保留在网站端，App 不执行未经确认的发布或删除操作。
- 新增 1.1.0/1.2.0 账户功能与消息中心详细实施计划，记录精确 API、兼容字段、验收门槛和后续遗漏功能。
- 新增账户资料、用户关系、阅读记录、发布作品及分页兼容的单元测试。

### 改进

- 书籍封面解析新增 `image`、`book_cover`、`pic_url`、`pic` 等历史字段回退。
- 登录功能页面统一空状态、错误重试、加载更多和操作中防重复提交体验。
- 默认版本升级至 `versionName 1.1.0`、`versionCode 4`。

### API

- `api/bff/my-home-v1`：个人概览。
- `api/bff/user-following-v1`、`api/bff/user-followers-v1`：关注与粉丝列表。
- `api/bff/toggle-user-follow-v1`：关注关系切换。
- `api/bff/history-v1`、`api/new-content-read/delete-book-history`：阅读记录与删除。
- `api/bff/longform-book-list-v1`：发布作品管理列表。

### 验证

- `testDebugUnitTest`、`lintDebug`、`assembleDebug` 通过。
- 小米 25042PN24C、Android 16 真机验证通过：
  - 登录用户 `jyy0736` 的个人资料、骑士用户组、轻币与统计正确显示。
  - 3 个关注正常加载；0 粉丝正确显示空状态。
  - 阅读记录成功加载，并出现多条“继续阅读”入口。
  - 0 个发布作品正确显示空状态。
- 真机验收未执行关注、取消关注或删除记录，不改变用户账户数据。

### 已知限制

- 发布管理为安全的作品状态视图，不包含新建/编辑/删除书籍、分卷、章节和草稿。
- 个人资料编辑、头像/背景、勋章中心、动态社区与消息中心尚未实现；消息中心已规划为 1.2.0。

## [1.0.2] - 2026-08-07

### 阅读器

- 重做分页阅读布局：正文使用除系统安全区域外的完整可用屏幕，不再为标题栏和菜单栏预留大块上下空白。
- 标题栏与底部菜单默认隐藏；点击屏幕中心时以浮层覆盖正文显示，再次点击即可收起，不中断当前阅读位置。
- 修复“上一章”和“下一章”按钮无响应的问题，章节导航会依据整本书的卷、章顺序定位相邻章节。
- 在章节最后一页继续向后翻时自动进入下一章；到达整本书末尾时给出明确提示，减少连续阅读被打断的情况。
- 新增页码输入跳转和可快速拖动的阅读进度条，并在跳转后保持页码、正文与章节进度同步。
- 页码改为底部水平居中显示，避免被全面屏的大圆角、手势区域或屏幕边缘遮挡。
- 使用 Android Window Insets 获取状态栏、刘海和打孔区域的真实高度，正文不会绘制到不可见区域；状态栏仍由系统正常显示。
- 加强阅读设置按钮的选中态配色和边框反馈，使字体、字号、背景、行高和阅读模式的当前选择更易辨认。

### 图片与网络

- 修复书籍封面和章节插图无法加载的问题，图片请求与 API 请求统一复用支持 HTTP/3/QUIC 的 Cronet 网络栈。
- 按原站正文结构解析 `[res]资源类型,资源编号[/res]` 标签，恢复插图在正文中的原始顺序，并兼容 `body_html` 中的图片节点。
- 扩充封面、章节和目录响应的历史字段兼容，避免接口字段名称变化时页面只显示占位图。
- 针对部分中国大陆网络环境中的连接重置、CDN 节点不可达和瞬时 5xx，加入受限重试、Cronet 引擎重建及备用边缘地址轮换。

### 工程与质量

- 新增 GitHub Actions Android CI，在 PR 与主分支提交上执行 `testDebugUnitTest`、`lintDebug` 和 `assembleDebug`。
- 新增跨卷章节导航、正文资源解析、图片字段兼容和网络响应解析测试。
- 默认版本升级至 `versionName 1.0.2`、`versionCode 3`。

### 验证

- 在小米 Android 16 真机验证分页翻页、上下滚动、菜单显隐、页码跳转、前后章节跳转、末页自动续章、刘海/状态栏安全区与底部居中页码。
- 验证首页封面、书籍详情封面与含 `[res]...[/res]` 的章节插图均可正常加载。

## [1.0.0] - 2026-08-06

### 首次发布：账户与内容浏览

- 支持用户名或邮箱加密码登录、邮箱验证码注册、会话恢复和安全退出。
- 将站点 `security_key` 使用 Android Keystore 加密后保存在本机；应用不保存密码和邮箱验证码。
- 登录错误由站点错误码映射为可理解的中文提示，可区分用户名/密码错误、验证码错误、网络连接失败和服务端异常。
- 首页提供热门、排行、新书、原创、同人、EPUB、最近更新等分区，并支持分区列表跳转与分页加载。
- 支持关键词搜索、分类与标签筛选，并可从结果直接进入书籍详情。
- 书籍详情展示封面、作者、简介、标签、连载状态、统计信息、同书其他版本、分卷和章节目录。
- 登录后可加入或移出书架，并在“我的书架”查看收藏作品。
- 评论区提供匿名只读展示；评论接口故障不会阻断书籍详情和正文阅读。

### 首次发布：阅读器

- 支持“书籍 → 分卷 → 章节”三级目录和章节阅读，并保存本地阅读位置；登录用户同步站点阅读进度。
- 默认使用贴近常见阅读软件的左右分页模式，支持点击左右区域和横向滑动翻页；同时保留上下滚动模式。
- 支持无衬线、衬线、等宽三种字体，14–32sp 字号，以及行高、页边距调整。
- 提供白色、米黄、护眼绿和深色四种阅读背景，阅读偏好使用 DataStore 持久化。
- 解析原站 `body_html` 正文及 `[res]...[/res]` 资源标签，将章节插图按原文顺序与文字混排显示。
- 支持上一章、下一章、返回目录和当前章节进度显示。

### 网络与兼容

- 根据轻之国度 Web BFF 实际接口实现统一 Repository、响应信封解包、分页模型和历史字段兼容解析。
- API 与图片统一使用嵌入式 Cronet，优先通过 HTTP/3/QUIC 建立连接，改善部分运营商网络下系统网络栈连接失败的问题。
- Gradle Wrapper 使用腾讯云分发镜像；Google Maven、Maven Central 和 Gradle Plugin Portal 优先使用中国大陆镜像并保留官方源回退。
- 明确处理站点合集频道处于维护状态的情况，不调用猜测接口；通过分卷目录和同书其他版本覆盖可用阅读结构。

### 发布与项目资料

- 建立 Kotlin、Jetpack Compose、Material 3、Navigation Compose 与分层 Repository/ViewModel 工程结构，最低支持 Android 8.0（API 26）。
- Release 构建强制要求 Android Keystore 签名，提供 Windows 交互式签名配置脚本，并将密钥与密码排除在 Git 之外。
- 新增 GitHub Actions Release 流水线：标签触发测试、Lint、签名构建、`apksigner` 校验、APK 与 SHA-256 文件发布。
- 设计单色底、含“LK”字母元素的应用图标，补充发现页、分页插图阅读和滚动阅读截图。
- 提供完整 README、API/架构/验收实施计划和 MIT License。

### 验证

- 单元测试覆盖登录错误映射、首页/搜索/书籍/章节/评论响应解析和正文资源标签解析。
- 在小米 Android 16 真机完成登录、会话恢复、分区、搜索、详情、书架、章节目录、分页/滚动阅读、字体字号、背景和插图显示验证。
